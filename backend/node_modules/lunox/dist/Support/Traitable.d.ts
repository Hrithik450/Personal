declare type Trait<Base = any, T = any> = (s: Base) => T;
declare const Traitable: <T>(superclass: T) => TraitBuilder<T>;
declare class TraitBuilder<T> {
    superclass: T;
    constructor(superclass: T);
    use(...traits: Trait[]): T;
}
export { type Trait, Traitable };
