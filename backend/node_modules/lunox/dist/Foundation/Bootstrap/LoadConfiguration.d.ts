import type { Bootstrapper } from "../../Contracts/Foundation/Boostrapper";
import type Application from "../Application";
import Repository from "../../Config/Repository";
declare class LoadConfiguration implements Bootstrapper {
    bootstrap(app: Application): Promise<void>;
    getConfigurationFiles(app: Application): Promise<string[]>;
    loadConfigurations(app: Application, repository: Repository): Promise<void>;
}
export default LoadConfiguration;
