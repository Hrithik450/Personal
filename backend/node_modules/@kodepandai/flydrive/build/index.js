import { RuntimeException as t } from "node-exceptions";
import * as e from "fs-extra";
import { promises as r } from "fs";
import {
  resolve as s,
  join as i,
  sep as n,
  dirname as a,
  relative as o,
} from "path";
import { promisify as c } from "util";
import { pipeline as h } from "stream";
/**
 * @kodepandai/flydrive
 *
 * @license MIT
 * @copyright Slynova - Romain Lanz <romain.lanz@slynova.ch>
 */ class u extends t {
  constructor(t, e) {
    super(
      `Unauthorized to access file ${e}\n${t.message}`,
      500,
      "E_AUTHORIZATION_REQUIRED"
    ),
      (this.raw = t);
  }
}
/**
 * @kodepandai/flydrive
 *
 * @license MIT
 * @copyright Slynova - Romain Lanz <romain.lanz@slynova.ch>
 */ class d extends t {
  static driver(t) {
    const e = new this(`Driver ${t} is not supported`, 400);
    return (e.driver = t), e;
  }
}
/**
 * @kodepandai/flydrive
 *
 * @license MIT
 * @copyright Slynova - Romain Lanz <romain.lanz@slynova.ch>
 */ class w extends t {
  constructor(t, e) {
    super(`The file ${e} doesn't exist\n${t.message}`, 500, "E_FILE_NOT_FOUND"),
      (this.raw = t);
  }
}
/**
 * @kodepandai/flydrive
 *
 * @license MIT
 * @copyright Slynova - Romain Lanz <romain.lanz@slynova.ch>
 */ class l extends t {
  static missingDiskName() {
    return new this(
      "Make sure to define a default disk name inside config file",
      500,
      "E_INVALID_CONFIG"
    );
  }
  static missingDiskConfig(t) {
    return new this(
      `Make sure to define config for ${t} disk`,
      500,
      "E_INVALID_CONFIG"
    );
  }
  static missingDiskDriver(t) {
    return new this(
      `Make sure to define driver for ${t} disk`,
      500,
      "E_INVALID_CONFIG"
    );
  }
  static duplicateDiskName(t) {
    return new this(
      `A disk named ${t} is already defined`,
      500,
      "E_INVALID_CONFIG"
    );
  }
}
/**
 * @kodepandai/flydrive
 *
 * @license MIT
 * @copyright Slynova - Romain Lanz <romain.lanz@slynova.ch>
 */ class f extends t {
  constructor(t, e) {
    super(
      `Method ${t} is not supported for the driver ${e}`,
      500,
      "E_METHOD_NOT_SUPPORTED"
    );
  }
}
/**
 * @kodepandai/flydrive
 *
 * @license MIT
 * @copyright Slynova - Romain Lanz <romain.lanz@slynova.ch>
 */ class p extends t {
  constructor(t, e) {
    super(
      `The bucket ${e} doesn't exist\n${t.message}`,
      500,
      "E_NO_SUCH_BUCKET"
    ),
      (this.raw = t);
  }
}
/**
 * @kodepandai/flydrive
 *
 * @license MIT
 * @copyright Slynova - Romain Lanz <romain.lanz@slynova.ch>
 */ class m extends t {
  constructor(t, e) {
    super(
      `Missing permission for file ${e}\n${t.message}`,
      500,
      "E_PERMISSION_MISSING"
    ),
      (this.raw = t);
  }
}
/**
 * @kodepandai/flydrive
 *
 * @license MIT
 * @copyright Slynova - Romain Lanz <romain.lanz@slynova.ch>
 */ class _ extends t {
  constructor(t, e, r) {
    super(
      `An unknown error happened with the file ${r}.\nPlease open an issue at https://github.com/kodepandai/flydrive/issues\n\nError code: ${e}\nOriginal stack:\n${t.stack}`,
      500,
      "E_UNKNOWN"
    ),
      (this.raw = t);
  }
}
/**
 * @kodepandai/flydrive
 *
 * @license MIT
 * @copyright Slynova - Romain Lanz <romain.lanz@slynova.ch>
 */ class g extends t {
  constructor(t, e) {
    super(
      `The key path does not exist: ${e}\n${t.message}`,
      500,
      "E_WRONG_KEY_PATH"
    ),
      (this.raw = t);
  }
}
class k {
  append(t, e) {
    throw new f("append", this.constructor.name);
  }
  copy(t, e) {
    throw new f("copy", this.constructor.name);
  }
  delete(t) {
    throw new f("delete", this.constructor.name);
  }
  driver() {
    throw new f("driver", this.constructor.name);
  }
  exists(t) {
    throw new f("exists", this.constructor.name);
  }
  get(t, e) {
    throw new f("get", this.constructor.name);
  }
  getBuffer(t) {
    throw new f("getBuffer", this.constructor.name);
  }
  getSignedUrl(t, e) {
    throw new f("getSignedUrl", this.constructor.name);
  }
  getStat(t) {
    throw new f("getStat", this.constructor.name);
  }
  getStream(t) {
    throw new f("getStream", this.constructor.name);
  }
  getUrl(t) {
    throw new f("getUrl", this.constructor.name);
  }
  move(t, e) {
    throw new f("move", this.constructor.name);
  }
  put(t, e, r) {
    throw new f("put", this.constructor.name);
  }
  prepend(t, e) {
    throw new f("prepend", this.constructor.name);
  }
  flatList(t) {
    throw new f("flatList", this.constructor.name);
  }
}
/**
 * @kodepandai/flydrive
 *
 * @license MIT
 * @copyright Slynova - Romain Lanz <romain.lanz@slynova.ch>
 */ function y(t) {
  return (
    null !== t &&
    "object" == typeof t &&
    "function" == typeof t.pipe &&
    "function" == typeof t._read &&
    "object" == typeof t._readableState &&
    !1 !== t.readable
  );
}
const D = c(h);
/**
 * @kodepandai/flydrive
 *
 * @license MIT
 * @copyright Slynova - Romain Lanz <romain.lanz@slynova.ch>
 */ function v(t, e) {
  switch (t.code) {
    case "ENOENT":
      return new w(t, e);
    case "EPERM":
      return new m(t, e);
    default:
      return new _(t, t.code, e);
  }
}
class $ extends k {
  constructor(t) {
    super(), (this.$root = s(t.root));
  }
  _fullPath(t) {
    return i(this.$root, i(n, t));
  }
  async append(t, r) {
    try {
      return { raw: await e.appendFile(this._fullPath(t), r) };
    } catch (e) {
      throw v(e, t);
    }
  }
  async copy(t, r) {
    try {
      return { raw: await e.copy(this._fullPath(t), this._fullPath(r)) };
    } catch (e) {
      throw v(e, `${t} -> ${r}`);
    }
  }
  async delete(t) {
    try {
      return { raw: await e.unlink(this._fullPath(t)), wasDeleted: !0 };
    } catch (e) {
      const r = v(e, t);
      if (r instanceof w) return { raw: void 0, wasDeleted: !1 };
      throw r;
    }
  }
  driver() {
    return e;
  }
  async exists(t) {
    try {
      const r = await e.pathExists(this._fullPath(t));
      return { exists: r, raw: r };
    } catch (e) {
      throw v(e, t);
    }
  }
  async get(t, r = "utf-8") {
    try {
      const s = await e.readFile(this._fullPath(t), r);
      return { content: s, raw: s };
    } catch (e) {
      throw v(e, t);
    }
  }
  async getBuffer(t) {
    try {
      const r = await e.readFile(this._fullPath(t));
      return { content: r, raw: r };
    } catch (e) {
      throw v(e, t);
    }
  }
  async getStat(t) {
    try {
      const r = await e.stat(this._fullPath(t));
      return { size: r.size, modified: r.mtime, raw: r };
    } catch (e) {
      throw v(e, t);
    }
  }
  getStream(t) {
    return e.createReadStream(this._fullPath(t));
  }
  async move(t, r) {
    try {
      return { raw: await e.move(this._fullPath(t), this._fullPath(r)) };
    } catch (e) {
      throw v(e, `${t} -> ${r}`);
    }
  }
  async prepend(t, e) {
    try {
      const { content: r } = await this.get(t, "utf-8");
      return this.put(t, `${e}${r}`);
    } catch (r) {
      if (r instanceof w) return this.put(t, e);
      throw r;
    }
  }
  async put(t, r) {
    const s = this._fullPath(t);
    try {
      if (y(r)) {
        const t = a(s);
        await e.ensureDir(t);
        const i = e.createWriteStream(s);
        return await D(r, i), { raw: void 0 };
      }
      return { raw: await e.outputFile(s, r) };
    } catch (e) {
      throw v(e, t);
    }
  }
  flatList(t = "") {
    const e = this._fullPath(t);
    return this._flatDirIterator(e, t);
  }
  async *_flatDirIterator(t, e) {
    const s = t[t.length - 1] === n ? t : a(t);
    try {
      const a = await r.opendir(s);
      for await (const r of a) {
        const a = i(s, r.name);
        if (a.startsWith(t))
          if (r.isDirectory()) yield* this._flatDirIterator(i(a, n), e);
          else if (r.isFile()) {
            const t = o(this.$root, a);
            yield { raw: null, path: t };
          }
      }
    } catch (t) {
      if ("ENOENT" !== t.code) throw v(t, e);
    }
  }
}
/**
 * @kodepandai/flydrive
 *
 * @license MIT
 * @copyright Slynova - Romain Lanz <romain.lanz@slynova.ch>
 */ class E {
  constructor(t) {
    (this._disks = new Map()),
      (this._drivers = new Map()),
      (this.defaultDisk = t.default),
      (this.disksConfig = t.disks || {}),
      this.registerDriver("local", $);
  }
  getDisks() {
    return this._disks;
  }
  getDrivers() {
    return this._drivers;
  }
  disk(t) {
    if (!(t = t || this.defaultDisk)) throw l.missingDiskName();
    if (this._disks.has(t)) return this._disks.get(t);
    const e = this.disksConfig[t];
    if (!e) throw l.missingDiskConfig(t);
    if (!e.driver) throw l.missingDiskDriver(t);
    const r = this._drivers.get(e.driver);
    if (!r) throw d.driver(e.driver);
    const s = new r(e.config);
    return this._disks.set(t, s), s;
  }
  addDisk(t, e) {
    if (this.disksConfig[t]) throw l.duplicateDiskName(t);
    this.disksConfig[t] = e;
  }
  registerDriver(t, e) {
    this._drivers.set(t, e);
  }
}
export {
  u as AuthorizationRequired,
  d as DriverNotSupported,
  w as FileNotFound,
  l as InvalidConfig,
  $ as LocalFileSystemStorage,
  f as MethodNotSupported,
  p as NoSuchBucket,
  m as PermissionMissing,
  k as Storage,
  E as StorageManager,
  _ as UnknownException,
  g as WrongKeyPath,
  y as isReadableStream,
  D as pipeline,
};
